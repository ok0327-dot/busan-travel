name = "busan-travel"
compatibility_date = "2026-01-01"

# Workers Static Assets — frontend/public/ 을 정적 사이트로 서빙.
# Worker 스크립트는 없음 (main 필드 생략 → assets-only 모드).
# SPA 라우팅 아니지만 단일 HTML 에 해시 라우팅 없으므로 기본 동작 OK.
[assets]
directory = "./frontend/public"
not_found_handling = "404-page"
